# Focused optimization checkpoint

The active generator retains only route parameters while ranking placements and reconstructs deterministic turn samples only for visited plans. Removed unreachable six-corridor/order/module branches; the seeded random draws and emitted diagnostic fields remain unchanged. Unreal tube construction reuses eight exact double trigonometric pairs, and the HUD draws its already split status/telemetry lines. The inspection plot groups coordinates once while retaining sorted IDs and input point order.

Three real requests (flat42, hills9, canyon42) were generated by the frozen previous executable and the rebuilt executable in alternating order. Their plans, traces and saved files match byte for byte. Report objects match after excluding generationSeconds. Each save uses the existing independent replay validation. The comparison script and all raw outputs are local alongside this report.

| Case | Before generation | After generation | Before peak working set | After peak working set |
|---|---:|---:|---:|---:|
| flat42 | 12.509 s | 12.404 s | 77,807,616 B | 74,002,432 B |
| hills9 | 15.751 s | 15.790 s | 78,520,320 B | 74,530,816 B |
| canyon42 | 15.237 s | 14.809 s | 77,246,464 B | 74,407,936 B |

One run per executable/case establishes exact output preservation and lower observed process memory, not a statistically established latency improvement. No game FPS claim follows. The plotting agent passed all 20 inspection tests and compared actual full RGBA output, mappings and metrics against HEAD; all matched. Its synthetic grouping-only timing is not total plotting performance.

The actual Unreal editor target and all seven existing contracts passed via the standard PrepareOnly script, with no automation override. Evidence: native/unreal/Saved/BuildRuns/20260910-060624-120. MeshContract compares generated rail/spine vertices, normals and UVs with an independently recomputed reference.

All 26 CTest suites passed in 406.46 s. An actual editor-game flat42 load/traversal completed in 166.193750 s with pause/restart/load, missing-reference refusal and save cancellation checks. Six real captures were opened: multiline status and telemetry remain readable without overlap, and the terminal status panel contracts correctly. This load-only run does not cover ordinary save, telemetry-off toggling, accepted setup overlay, keyboard input or FPS. See editor-flat42-front/REVIEW.md. No published package has been replaced. This change preserves the known terrain routing shortcomings; subsequent terrain engineering is a separate behavioral change with separate evidence.
