# Final packaged verification

Runtime 0.8.2-terrain.1, source 6f23a9e, package `native/unreal/Packaged/run-20260910-072651-235/Windows`. This package was built, cooked, staged and archived with all seven Unreal contracts passing. The subsequent core fixture edit changes only a historical candidate-index assertion, not the packaged implementation.

Two fresh-profile packaged runs generated their own accepted rides (no preloaded save). Flat42 rear completed in 164.755208 s and canyon9 front in 207.575 s. Both `capture/result.json` files report full traversal, ordinary save/load, pause/restart, paused pose, save cancellation and missing-reference refusal passing, at 2560x1440. Keyboard input and other cancellation phases are not covered. Concurrent runs are not a performance benchmark.

Actually inspected seven final-package frames: canyon9 `01-overview`, `06-pov-3`, `09-pov-6`, `23-pov-20`, `26-terminal`; flat42 `10-pov-7`, `18-pov-15`. The canyon loop approach is near local ground (displayed 6.4 m at 143.5 km/h), the return descends the cliff and arrives straight in the station. The overview still exposes a long folded footprint and substantial neutral approaches. Tall trusses dominate the signature structures; cliff shadows obscure the descent. Flat rear views show the conventional train but little scenery to convey speed. No obvious track break appears in these selected frames; still images cannot establish continuous roll/jerk quality or every clearance.

The automated JSON keeps its original human-review-pending field; this separate review records what the assistant actually viewed without rewriting raw evidence. This is a playable engineering checkpoint, not full POV approval, a structural/F2291 assessment, an intensity record, Mac runtime validation or a 60 fps result.

The fresh distribution ZIP passed CRC and copy-hash verification, and all 48 runtime files matched after extraction. The extracted inner game and root bootstrap both opened the game; both startup screenshots were inspected at desktop-native 1920x1080. Console queries confirm MaxFPS 0, VSync 0 and ScreenPercentage 100. The root-bootstrap harness initially tried to close the launcher parent rather than its game child; the exact extracted child window was then identified and closed normally. This harness failure and follow-up are preserved separately. The ZIP is 338,574,334 bytes, SHA256 `23277570ed9fae6b8eb7ec34a6de43b039eb7166bad890a6fc972751634b7fe8`. Distribution tooling's two existing tests pass.
