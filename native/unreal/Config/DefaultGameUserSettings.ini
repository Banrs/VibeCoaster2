[/Script/Engine.GameUserSettings]
; Match the installed UE 5.8 user-settings schema so first launch retains defaults.
Version=5
; Bound ordinary rendering to the game's 60 fps target, including shared-GPU use.
; A saved user setting or an explicit console override can choose another limit.
FrameRateLimit=60.000000
