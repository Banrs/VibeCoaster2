# Preserved pre-rollback inputs

These files are byte-for-byte copies of the ride/fixture inputs present before restoring `0.8.0-immelmann.2`. They were not regenerated or converted. Several historical inputs are intentionally header-only rejection fixtures; `legacy-v091-adaptive-seed38.coaster` contains later canonical geometry and has its provenance alongside it.

The restored application's older save format may reject these files. Its matching full historical test fixtures remain in `native/core/tests/fixtures`. Copy hashes and the complete prior workspace are retained in `D:/Coding/Codex/vibecoasterlegacy/paused-milestone-20260916`.
